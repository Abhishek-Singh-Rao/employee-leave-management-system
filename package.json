{
  "name": "employee-leave-management-system",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@sap/cds": "^9.3.1",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.0.2"
  },
  "scripts": {
    "start": "cds watch"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "leave.db"
        }
      }
    }
  }
}
